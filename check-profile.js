require('dotenv').config({ path: '.env.local' }); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function checkProfile() { const EMAIL = 'artherasmg@gmail.com'; console.log('Checking profiles for:', EMAIL); const { data, error } = await supabase.from('profiles').select('*').eq('email', EMAIL); console.log(error ? 'Error: ' + error.message : 'Found profiles: ' + JSON.stringify(data)); } checkProfile();
